const blogData = require('./blog');
const commentData = require('./comment');
const userData = require('./user');

module.exports = {
    blog: blogData,
    user: userData,
    comment: commentData
};